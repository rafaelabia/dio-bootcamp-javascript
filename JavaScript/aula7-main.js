const funcoes = require('./aula7-funcoes-auxiliares');

console.log(funcoes);

/*object destructuring: 
const { gets, print } = require('./aula7-funcoes-auxiliares');

const pessoa = {
  nome: 'vitor'
}

Equivalentes:
const { nome } = pessoa;
const nome = pessoa.nome; 
*/
